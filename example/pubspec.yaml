name: flutter_mediator_example
description: Example project for flutter_mediator

publish_to: "none" # Remove this line if you wish to publish to pub.dev

version: 1.2.0

environment:
  sdk: ">=2.12.0-0 <3.0.0"

dependencies:
  cupertino_icons: ^0.1.3
  flutter:
    sdk: flutter

  #  flutter_i18n: ^0.20.1
  #  flutter_mediator: ^1.1.2+3
  flutter_mediator:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  #assets:
  #- assets/
  #- assets/flutter_i18n/
