name: flutter_mediator
description: Flutter mediator is a state management package base on the InheritedModel with automatic aspect management to make them simpler, easier, and intuitive to use.
version: 2.1.0
homepage: https://github.com/rob333/flutter_mediator

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  # pedantic: ^1.9.0
  flutter_test:
    sdk: flutter

flutter:
